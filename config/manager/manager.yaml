---
apiVersion: v1
kind: Namespace
metadata:
  name: tinycode-operator-system
  labels:
    app.kubernetes.io/name: tinycode-operator
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tinycode-operator-manager
  namespace: tinycode-operator-system
  labels:
    app.kubernetes.io/name: tinycode-operator
    app.kubernetes.io/component: manager
    control-plane: controller-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      control-plane: controller-manager
  template:
    metadata:
      labels:
        control-plane: controller-manager
        app.kubernetes.io/name: tinycode-operator
      annotations:
        kubectl.kubernetes.io/default-container: manager
    spec:
      serviceAccountName: tinycode-operator-manager
      # Operator runs fully restricted — no host access, dropped ALL caps
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
      containers:
        - name: manager
          image: quay.io/bjohns/tinycode-operator:latest
          imagePullPolicy: IfNotPresent
          # No command override — use image ENTRYPOINT (python3.11 /app/main.py)
          ports:
            - name: metrics
              containerPort: 8383
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          env:
            - name: WATCH_NAMESPACE
              value: ""  # Empty = cluster-wide watch
            - name: OPERATOR_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: HELM_CHART_PATH
              value: /helm-charts/tinycode
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 10m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
